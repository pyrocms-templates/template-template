{
  "schema": {
    "vendor": {
    "type": "string",
    "required": true,
    "label": "Vendor name",
    "default": "{{vendor}}"
    },
    "slug": {
      "type": "string",
      "required": true,
      "label": "Addon slug",
      "default": "{{slug}}"
    },
    "description": {
      "type": "string",
      "required": false,
      "label": "Description",
      "default": "{{description}}"
    }
  }
}
